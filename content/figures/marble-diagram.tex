\begin{figure}
  \centering
  \begin{tikzpicture}
    \tikzstyle{marble}=[circle, draw, fill=white, line width=0.3mm, inner sep=2mm]

    \node (a) [
        style=marble
    ] {0};
    \node (b) [
        style=marble,
        right=4mm of a
    ] {2};
    \node (c) [
        style=marble,
        right=4mm of b
    ] {4};
    \node (d) [
        style=marble,
        right=4mm of c
    ] {6};

    \node (x) [
        right=4mm of d
    ] {};
    \node (y) [
        above=2mm of d
    ] {};
    \node (z) [
        below=2mm of d
    ] {};

    \path[-,draw] (a) edge (b);
    \path[-,draw] (b) edge (c);
    \path[-,draw] (c) edge (d);
    \path[->,draw] (d) edge (x);

    \path[-,draw] (z) edge (d);
    \path[-,draw] (y) edge (d);
  \end{tikzpicture}
  \caption{Marble Diagram visualizing the observable in Listing \ref{lst:rp-program}. From left to right, each marble represents an emitted event. The vertical line at the last marble indicates a complete event after emitting the last next carrying the value 6.}
  \label{fig:marble-diagram}
\end{figure}
